# This file describes how far we've got at the time this distribution was created.
# 0-4 shows amount of "Doneness" 
# - means done.

*	Pre-Alpha - ie. You can't install it without a telepathic connection to Chris. 

-	Distribution Installer
-	Distribution Creator
-	"Create New Archive" Script
-	Tool to manage XML Phrase files and spot missing bits.
-	Tool to manage XML Template files
-	Archive Apache Config Maker (still needs to do virtualhosts)
-	System Apache Config Maker
-	languages.xml file to configure what languages EPrints has heard of
-	XML config files to tell eprints where to find each archive
-	Multi-Lang fields in DB
-	Alpha Install Docs.
-	Executable files should check they are running as the correct user.

*	Milestone "Anchovy" : ALPHA Release - ie. You can download and install it, but it won't be actually useful yet. (August 1st 2001) [YOU ARE HERE]

-	Names need 4 fields: Honourific Givennames Familynames Lineage. For Sir Richard Thompson Jnr. or similar names.
-	Multilangify Subject Fields and Subject DB
-	"ID" fields for ISBN/userid etc.
-	Web-based Registration/password change
-	Get the Deposit Item form working
-	Turn off caching of on-the-fly pages?
-	Generate abstract page routine will create symlinks to actual docs (if appropriate)
-	Security -> config,edit,authz [used for unfinished items too?]
-	Multilang. Config for mails and submission agreement.
-	"Magic" Next & Prev in SubmissionForm
-	Multi-Lang fields in HTML
4	Field Value Renderer 
-	Best version sub for multilang fields (picks the best choice of language when a field has several set)
-	Skippable Stages in SubmissionForm, and multiple page metadata form.
-	Phrasify Option Fields
-	Editor Pages -approve,bounce,edit etc.
-	Mailer (Unicode friendly?)
-	Indexcode generator in ArchiveConfig.pm is UTF-8 clean
- 	Super all-singing/dancing etc. Subject Editor
-	Numeric fields (not prefix'd)
-	Move as much as possible of config into XML conf file.
-	Views (and searches?) with JUST results - no wrapper. For including in other pages.


*	Milestone "Pepperoni" : Database code stable. Modules, scripts and CGI still unstable.

#	always use encrypted passwords
#	Fix "upload then back" bug
#	Default Action when buttons pushed
#	Fix Buttons on search results page.
#	Bug: Why does 'help' disappear when there is a problem with a search?
#	Clean up validators - Don't validate records with fields which have bad langids including user validate.
#	Order buttons somehow
#	Staff EPrint Edit
#	Threads rendering code.
#	Staff User Edit/Create
#	Delete records function.
#	Normalise names for things between XML config, DTD and Perl modules... eg "server_static"
#	Make all possible field-types browseable.
#	Make SearchFields have a better call method.
#	Get install upgrader working properly.
#	"Squash" reverse indexes?
#	rationalise names of cgi and bin scripts ?
#	clone cocks up later version id
#	/users/perl/home/ is legal and should not be -> should redir to /users/perl/home, and/or make all URL links absolute.
#	"secret" field should never have a default set in forms, but no value should not erase previous value.
#	lose "bomb"
#	Put System field titles in system phrase file.
#	Bug: 500 error when search for only ignored words.
# 	"Bad archive ID" on bin scripts rather than weird crash.
#	print STDERR's should be log()

#	Eliminate HTMLRenderer

0	Browse Multiple Fields at once (eg. authors.id/editors.id)
0	Cosmetic views of broswed fields. Eg. authors.id renders as their name...
0	Paged Metadata in submission form.
0	Alerts for editors about things they can edit (flag in User fields?)
2	Subject-editors (editors can only edit within certain subjects)
2	Web-based Email Change
0	Password Encryption. (Via callback)
2	Autodetect optional system features: microtime, df etc.
0	PDF Mollycoddler ( Serve PDF in a special way which avoids certain bugs in the plugin )
0	Field property: freetext (or not-freetext) to stop indexing)
0	Field Property: 'id required' - If main bit is set then ID *must* be set. Otherwise lax.
-	Option to hide Honourific and/or Lineage.
0	Commentary summary page (Psycoloquy style)
-	Make the mailer only encode UTF-8 in Mime Headers if needed, and (possibly) try and use ISO-LATIN-1 if possible.
-	Cosmetic usernames via callbacks to Config.pm to munge and unmunge what the user edits - so you can edit the username, not the UID, for example.
4	Utils.pm module with all the general static methods. Including all of Name.pm and some of Session.pm and Config.pm and parse_xml?
-	multiple views for things like people/groups etc.
0	Optional "new" highlight in views/searches etc. (maybe done by eprint->render link).
0 	Metadata approve page in Submission Form for Admin Conf'd comments(?)
-	Search Expression Serialiser/Unserialiser - Needs to have guaranteed order to use in caches.
0	Multiple Citation formats (generally, and for browse/abstract/search)
-	Default Values for metadata
-	Subject field root is not always ROOT - can come into different places in the subject tree
0	Searching "ancestors" of a subject. eg. search for biology maps all subjects which are below biology.
0	Translate Initial phrases and templates into French
#!!!!!!!!!!!VVVVVV
0	Indexing on name fields(?)
0	patch for config files when upgrading.

*	Milestone "Olive" : Core code should be stable, scripts & cgi still not stable, core code may still change.

#	OAI support working.

0	Some kind of BibTeX support (fill in metadata form from bibtex)
0	Search by ID field (in search exp)
0	What's new? page(s) (just a fixed search)
0	Subscriptions - editing
0	Subscriptions - sending
0	MathML support
2	TeX mathmode support.
0	XML 1.1 Support(?)
1	Extra sets in OAI: By ID field, or by configurable serialised search. Even "subject" should be configured. so "subject:blgy:biophys" or "username_id:cjg" or "stuffjackwants" which is a search of it's own. Searches can of course be modified with from and to dates from OAI.
0	Taint all code to force sanity checking of all data
0	Probity (see www.probity.org for more info)
0	Content Negotiation with Browser
0	Keywords field appears in META keywords for search engines
0 	Clean up phrase file - nice and non-ambiguous.
0	remove all #cjg comments which denote issues.
0	Multipage search (cache code)
0	Multipage views
-	"Suckable" views for adding to external pages (eg. Staff CV/Resumes)
0	Other output charsets than UTF-8?
1	XML Importer
0	Improve name search syntax (?)
0	Remove (Hide) Lanuage related options if only one language supported.
0	Xanadu interface(!)
0	"Special search" which redirects to the abstract if only one match.


*	Milestone "Tomato" : BETA Release - ie. You can download it and install it, it should work, but has bugs. All features for 2.0 are (should) be stable. 

1	Comment Code
0	Bugfixing
0	Documentation!
0	How-To Documentation
0	Multiple Initial Configurations (eg Pre-print Archive, Institutional Archive etc)

*	Milestone "Double Cheese" : EPrints 2.0

# -	EPrints 1 : Make it do OAI 1.1
# -	Improve name search to handle ","s

# *	EPrints 1.1.2 OAI1.1 Compliance patch (August 28th 2001)

# 0	EPrints 1 : Make an exporter to dump database for import by ep2

# *	EPrints 1.2 Final Planned EPrints 1.* release.


1	Sleep
2	Beer
0	Peer Review
0	Mirroring
0	Citation Scanning
0	"People who read this eprint also read..."
1	OpenURL support (?)



# Files what do something: 
# . = not touched
# * = getting there
# - = "perfect"
# 46 files.

# bin/create_databases			*
# bin/create_user			*
# bin/erase_archive			*
# bin/generate_abstracts		*
# bin/generate_static			*
# bin/generate_views			*
# bin/list_user_emails			*
# bin/generate_dtd			*
# bin/rebuild_sets			.
# bin/subs_daily			.
# bin/subs_monthly			.
# bin/subs_weekly			.
# bin/update_htaccess			*
# bin/update_laf			 ?
# bin/import_xml			*
# bin/startup.pl			*
# bin/generate_apacheconf		*
# bin/create_new_archive		*

# cgi/set_password			*
# cgi/advsearch				*
# cgi/handle_404			.
# NEED: cgi/handle_403
# cgi/oai				.
# cgi/remind				 ?
# cgi/search				*
# cgi/user				*
# cgi/user_eprints			*
# cgi/view_eprint			.
# cgi/confirm				*
# cgi/setlang				*

# cgi/users/tester			 ?
# cgi/users/home			*
# cgi/users/record			*
# cgi/users/review			*
# cgi/users/submit			*
# cgi/users/subscribe			.
# cgi/users/status			*

# cgi/users/staff/add_user		.
# cgi/users/staff/buffer		*
# cgi/users/staff/edit_eprint		.
# cgi/users/staff/edit_submission	*
# cgi/users/staff/edit_user		.
# cgi/users/staff/eprint_search		*
# cgi/users/staff/user_search		*
# cgi/users/staff/view_submission	*
# cgi/users/staff/view_user		*
# cgi/users/staff/edit_subject		*

# lose cgi/remind
# render_input_form to use HASH params
