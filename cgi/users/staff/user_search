######################################################################
#
#  EPrints Users Search Form
#
#  Now deprecated. Use normal staff search for with /users on the end!
#
######################################################################
#
#  __COPYRIGHT__
#
# Copyright 2000-2008 University of Southampton. All Rights Reserved.
# 
#  __LICENSE__
#
######################################################################

use EPrints;

use strict;

my $session = new EPrints::Session;
exit( 0 ) unless( defined $session );

my $url = $session->get_repository->get_conf( "perl_url" )."/users/search/users";
if( $session->get_request->args )
{
	$url.= "?".$session->get_request->args;
}
$session->redirect( $url );

$session->terminate;
exit;

